{
  "name": "ason-agent",
  "version": "0.1.0",
  "description": "Ason Agent — 基于 Ollama 的本地智能体（零依赖，纯 Node 内置模块）",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "sql.js": "^1.14.1"
  }
}
