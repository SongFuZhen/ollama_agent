# 本地 Agent 客户端 — 产品设计书

> 版本：v0.1（基于《本地 Agent 客户端 — 项目构想》深化）
> 日期：2026-07-09

---

## 1. 产品概述

### 1.1 一句话定位
一个跑在**断网内网**上的本地 Web 应用：让弱小的 7B/14B 本地模型，通过"先读真数据、再回答"的 Agent 模式，做到**不瞎编、过程可见、可放心使用**的 AI 助手。

### 1.2 它是什么 / 不是什么
- ✅ 是：内网环境里唯一能用的、有约束的、不瞎编的 AI 助手
- ❌ 不是：Chatbox 的替代品（我们不追求自由聊天）
- ❌ 不是：Claude Code 的平替（不做全自动代码改动，重"辅助"而非"自主")

### 1.3 核心价值主张
> **弱模型 + 强约束 + 真数据 = 靠谱。** 不让模型凭空发挥，所有结论都锚定在用户本地的真实文件上。

---

## 2. 用户与场景

### 2.1 目标用户
| 角色 | 典型诉求 | 优先级 |
|---|---|---|
| 内网开发/运维 | 读代码、分析日志、定位异常 | P0 |
| 技术写手/办公 | 总结内部文档、起草报告 | P1 |

### 2.2 核心场景（内网断网）
1. **代码分析** — 读仓库文件，基于真实源码解释，不编造 API
2. **日志排查** — 读日志文件，定位异常时间点与报错栈
3. **文档总结** — 读内部技术文档，基于原文作答
4. **报告起草** — 用户提供大纲/模板，模型展开成文（仍基于给定材料）

---

## 3. 设计原则（约束即产品）

针对"7B 模型不可靠"这一根本约束，确立四条原则：

1. **模型只做"翻译"，不做"规划"**
   复杂多步任务由系统编排，模型只负责"读数据后说人话"。
2. **工具结果先摘要、再喂模型**
   大文件截断/摘要后注入，避免模型被长上下文淹没。
3. **危险操作人工确认**
   任何写/删文件，默认弹确认；可配置白名单目录免确认。
4. **每一步可追溯**
   思考链、工具调用、路径影响、耗时全部留痕展示。

---

## 4. 功能设计

### 4.1 信息架构（分场景三标签）
> 决策：采用**手动分页面**（三个标签页），不使用自动路由。理由：内网弱模型自判"该用哪个模型"不可靠，把路由判断交还给人，契合"模型只做翻译、不做规划"原则。

```
主界面（顶部三个标签页，各自独立会话 + 上下文）
├── [代码]  代码补全 / 解释代码      → qwen2.5-coder:7b
├── [排查]  复杂逻辑排查 / 找 bug     → deepseek-r1:8b（推理模型，自带 <think:6124c78e> 思维链）
├── [通用]  通用对话（非代码）        → qwen2.5:7b 或 llama3.1:8b
└── 每个标签内：
    ├── 对话区（消息流 + 工具调用时间线，隔离）
    └── 底部输入区（输入框 + 发送）
共享层（代码级复用，非运行时共享权限）：
    ├── Tool Registry 实现（sandbox 校验 + 文件读写，一份代码）
    └── 沙箱根目录 PROJECT_ROOT（单目录）
```

> 输入框弱提示（可选，纯前端规则，不调模型）：检测含代码块/路径/`bug`/`报错` 等关键词时，提示"可能想用：[排查]"，点击才切换。仅辅助，不自动路由。

### 4.2 功能清单（按版本）

#### V1（最小可用，必须成立）
| 功能 | 说明 | 关键约束 |
|---|---|---|
| 分场景三标签 | 代码 / 排查 / 通用，各自独立会话 | 模型固定、上下文隔离 |
| Agent 循环 | 思考→调工具→喂结果→再思考 | 带**失败重试**与**最大步数**保护 |
| 工具：只读集 | read_file / list_dir / search_files | 路径沙箱限制；V1 三页均仅开放只读 |
| Ollama 对接 | 按场景调对应本地模型 | 见 4.3 场景-模型映射 |
| 过程可视化 | 展示思考链 + 工具调用 | 前端时间线（R1 需剥离 <think:6124c78e> 块） |
| 路径沙箱 | 仅允许读写指定根目录 | 安全兜底 |
| 写操作确认 | 弹窗确认后方可写入 | **V1 不开放写工具**，见 4.4 |

#### V2（增强）
- 更多工具：write_file / list_dir / search_files / grep_log
- SSE 流式输出（实时思考过程）
- 多会话管理 + 会话切换
- 轻量短期记忆：自动摘要，避免长上下文崩
- 项目上下文钉定：把某目录设为默认读取范围

#### 远期
- Embedding + RAG：内部文档知识库（nomic-embed-text）
- 多模型切换
- 局域网部署，多人使用
- Docker 镜像化打包

---

## 4.3 场景 — 模型映射

| 场景（标签） | 绑定模型 | 模型特性 | 专属 system prompt 侧重 |
|---|---|---|---|
| 代码补全 / 解释 | `qwen2.5-coder:7b` | 代码专精 | 强调读文件后解释函数/模块，不编造 API |
| 复杂逻辑排查 / 找 bug | `deepseek-r1:8b` | **推理模型**，自带思维链 | 强调读日志、追调用链、定位异常根因 |
| 通用对话（非代码） | `llama3.1:8b` | 通用（能力强于 7B） | 文档总结、报告起草，弱化工具体 |

> 模型名可通过环境变量覆盖（`MODEL_CODER` / `MODEL_DEBUG` / `MODEL_GENERAL`）。
> 通用对话页默认 `llama3.1:8b`（通用能力优于 7B）；R1 的 `<think:6124c78e>` 块前端默认折叠、可展开；不做前端弱提示，纯手动切标签。
> 内网机器未必三个模型都拉取，启动时 preflight 探测存在性，缺模型时该标签置灰并提示"未安装"。

### 4.4 工具白名单（共享实现 + 按场景裁剪）

**原则**：工具代码实现**共享一份**（sandbox 校验 + 文件读写）；但**运行时可用工具集按场景裁剪**，贯彻"弱模型 + 最小权限"。

| 工具 | 实现 | V1 各场景可用？ | 说明 |
|---|---|---|---|
| `read_file` | 共享 | ✅ 代码 / 排查 / 通用 | 只读，沙箱限制 |
| `list_dir` | 共享 | ✅ 代码 / 排查 / 通用 | 只读 |
| `search_files` | 共享 | ✅ 代码 / 排查 / 通用 | 只读 |
| `write_file` | 共享 | ❌ V1 全禁 | 高风险，V2 起仅限专属"改写"场景，仍每次确认 |

**设计要点**
- **会话/上下文隔离**：三标签的对话历史与项目上下文相互独立，排查时读过的日志不污染代码页上下文。
- **V1 工具集 = 纯只读**，模型无写权限，杜绝 7B 误写风险。
- **V2 扩展**：引入独立"改写代码"场景（或排查页的显式"应用修改"按钮），才开放 `write_file`，且走每次单独确认。
- **白名单由后端按 scenario 注入**，前端无法越权调用未授权工具。

---

## 5. 关键流程设计

### 5.1 Agent 主循环（带防护）
```
用户输入（含 scenario 标签 → 选定模型 + 工具白名单）
  → 系统注入：角色约束 + 该场景可用工具 schema + 项目上下文
  → loop (max_steps = 6):
      1. LLM 思考，输出 {thought, action?, action_input?}
      2. （R1 模型）先剥离输出中的 <think:6124c78e>...</think:6124c78e> 块再解析
      3. 若无需工具 → 输出最终答案，结束
      4. 若需工具 → 校验 action 是否在【本场景白名单】
      5. 执行工具，结果做截断/摘要
      6. 结果喂回，回到 1
  → 超限未结束 → 强制收尾，提示"已达最大步数"
```
**防护点**：JSON 解析失败自动重试（最多 2 次）；工具执行异常不崩溃，返回错误文本给模型；循环步数硬上限 6；越权工具调用被拒绝。

### 5.1.1 R1 推理模型特殊处理
`deepseek-r1:8b` 输出含 `<think:6124c78e>推理过程</think:6124c78e>最终答案`，且可能把工具调用 JSON 包在 `<think:6124c78e>` 内。处理：
- 解析工具调用前，**先剥离** `<think:6124c78e>...</think:6124c78e>` 块，仅对剩余文本做 JSON 提取
- 前端展示时，`<think:6124c78e>` 内容归入"思考链"时间线并**默认折叠**（可点击展开）；最终答案单独成气泡
- 若 R1 在 `<think:6124c78e>` 内已隐含工具意图但格式不对，回落到重试/提示，不强制解析 think 内部

### 5.2 工具调用确认流（多次确认）
```
模型请求 write_file(path, content)
  → 前端弹出确认卡片：展示"将写入：<path>" + 内容预览
  → 用户确认 / 拒绝（每次写操作独立确认，可多次）
  → 结果回传模型继续循环
```
> 已决策：半自动形态，循环内**每次写操作单独弹确认**，不做一次性授权。

### 5.3 离线部署自检（preflight）
启动脚本 / 前端启动时检查：
- Ollama 进程是否在 `localhost:11434`
- **三场景模型是否都已拉取**（qwen2.5-coder:7b / deepseek-r1:8b / llama3.1:8b）；缺失则对应标签置灰并提示"未安装：xxx"
- node 版本是否满足（≥16）
任一项不满足 → 给出明确中文提示，而非直接报错崩溃。

---

## 6. 技术方案（沿用构想并补强）

### 6.1 架构
```
浏览器前端（纯 HTML/JS，三标签：代码/排查/通用，会话隔离）
   │ HTTP + SSE
Node.js 后端（零依赖，纯内置模块）
   ├── Scenario Router（选模型 + 注入工具白名单）
   ├── Agent Loop（带重试/步数上限 + R1 think 剥离）
   ├── Tool Registry（共享实现 + 沙箱路径校验 + 按场景裁剪）
   └── Memory（V2 会话摘要）
   │ HTTP POST localhost:11434
Ollama（按场景：qwen2.5-coder:7b / deepseek-r1:8b / llama3.1:8b）
```

### 6.2 离线打包方案（零依赖，U 盘直拷）
> **已决策**：移除 Express，改用 Node 内置 `http`，**零第三方依赖**，无 `node_modules`。内网已预装 Node 16，U 盘带走整个目录直接 `node src/server.js` 即可运行，无需 `npm install`、无需联网。

U 盘自包含包：
```
usb-package/
├── src/              （server.js / agent.js / tools.js / ollama.js / config.js）
├── public/           （前端三标签界面）
├── start.sh          （自检 + 启动）
├── README.md         （一行命令启动说明）
├── package.json      （无 dependencies）
└── ollama-models/    （Ollama 离线安装器 + 三个模型文件，需另随 U 盘携带并提前装好）
```
> 注意 Node 16 兼容性：不使用顶层 `await` 等 16 不支持的特性。

---

## 7. 安全设计

| 风险 | 措施 |
|---|---|
| 模型读取敏感路径（/etc、用户目录） | 路径白名单沙箱，仅允许配置的根目录 |
| 模型误写/覆盖重要文件 | 写操作默认人工确认；可配免确认白名单 |
| 工具调用循环失控 | 步数上限 + 超时 |
| 越权工具调用 | 后端按场景白名单校验，前端无法越权 |
| 审计需求 | 所有工具调用留痕日志（时间、路径、结果） |

---

## 8. 成功指标（内网可用即达标）
- [ ] V1 能在断网内网一键启动，无需联网、无需 npm install
- [ ] 三标签各自绑定正确模型，缺模型时该标签置灰提示
- [ ] 代码 / 日志排查场景：回答**可溯源到具体文件行**，无凭空编造
- [ ] V1 三页均无写权限，write_file 不在白名单内
- [ ] 单次任务在 6 步内收敛

---

## 9. 已决策汇总

| # | 决策 | 说明 |
|---|---|---|
| ① | 内网有 Node 16 | 不用 Docker |
| ② | 零依赖，U 盘直拷 | 移除 Express，纯 Node 内置 `http`，无 `node_modules` |
| ③ | 分场景三标签 + 手动选模型 | 不自动路由（弱模型自判不可靠） |
| ④ | 场景-模型映射 | 代码=qwen2.5-coder:7b / 排查=deepseek-r1:8b / 通用=llama3.1:8b |
| ⑤ | 共享工具实现 + 按场景裁剪白名单 | V1 三页均仅只读工具，write_file 进 V2 |
| ⑥ | 单目录沙箱 | 限定单个项目根路径读写 |
| ⑦ | 半自动多次确认 | 每次写操作单独弹确认（V1 暂未开放写工具） |

### 弱模型可靠性专项约束
- 强结构化 prompt + 宽松 JSON 解析 + 自动重试（最多 2 次）
- 步数上限 6，防空转烧 token
- 工具结果先截断/摘要再喂回
- R1 模型输出先剥离 `<think:6124c78e>` 块再解析

---

## 10. 与构想的差异说明
- **新增**：路径沙箱、写操作确认、循环防护、preflight 自检、安全设计、分场景三标签、工具白名单裁剪、R1 处理
- **修正**：离线部署矛盾（改为零依赖 U 盘直拷，而非内网 npm install / Docker）
- **强化**：针对弱模型，提出"模型只做翻译、工具结果先摘要、路由交还给人"的约束策略
- **已定**：Node 16 + 零依赖；三场景固定模型；沙箱单目录；半自动多次确认；V1 只读工具集
