# 1-UI-REVIEW.md — 重设计后 6 维视觉审计

> 审计对象：重设计后的 `public/index.html` / `style.css` / `app.js`（对照 `UI-SPEC.md`）。
> 评分 1–4（1=差 2=可用 3=良好 4=优秀）。

## 总览

| 维度 | 评分 | 结论 |
|---|---|---|
| 1. 视觉层次 | 4 | 顶栏/分段控件/对话三级清晰，语义色承担信息 |
| 2. 一致性 | 4 | 圆角、描边、主色、交互态统一 |
| 3. 间距与布局 | 4 | 固定顶栏+可折叠侧栏，密度可控 |
| 4. 色彩与对比 | 3 | 双主题对比达标，浅色下个别语义色偏暗 |
| 5. 字体排印 | 3 | 阶梯合理，长代码/路径换行待优化 |
| 6. 响应式与可达性 | 3 | 焦点环/aria 到位，窄屏侧栏与模型选择可再打磨 |

**综合：3.5 / 4** — 重设计达成「专业、克制、可信」目标，建议按下列项微调。

---

## 1. 视觉层次（4）
- 顶栏（品牌/状态/操作）→ 分段控件（场景）→ 对话（消息/步骤）→ 输入（模型/发送）层级分明。
- 状态胶囊用「点+文字」替代原来易溢出的纯文字，信息锚点清晰。
- 过程步骤用左侧 3px 语义色条 + 角色标签，区别于普通气泡，主线突出。
- 空状态提供欢迎语 + 示例卡，降低首屏空白焦虑。

## 2. 一致性（4）
- 全部圆角收束到 `--r / --r-sm / --r-pill`；按钮统一 `var(--accent)` 渐变或描边风格。
- hover/active/focus 态在文件树、标签、按钮上行为一致。
- 场景分段控件与状态胶囊共享同一套 surface/border 变量，视觉语言统一。

## 3. 间距与布局（4）
- CSS 变量统一 `topbar/tabs` 高度，主区 `flex:1 + min-height:0` 保证独立滚动，无双滚动条污染。
- 文件侧栏可折叠并记忆（`localStorage`），从 320px→0 平滑过渡，释放对话空间。
- 输入区改为「模型选择 + textarea + 发送」横向组合，比原纯 textarea 更紧凑专业。

## 4. 色彩与对比（3）
- 深色主题对比度普遍达标；状态点、语义色条辨识度好。
- 浅色主题下 `--amber:#d97706` 用于思考步骤文字时，长文小字对比略弱；建议步骤内文字仍用 `--text`、仅保留左侧色条表达语义（已部分采用）。
- 用户气泡白字在渐变上对比充足 ✅。

## 5. 字体排印（3）
- 字号阶梯 12/13/13.5/15/18 合理，正文 1.6 行高舒适。
- 等宽栈用于状态/路径/模型名，区分「机器信息」与「对话内容」✅。
- 待优化：`.step pre` 与长 JSON 路径在窄容器下横向滚动明显，可增加 `word-break`/换行提示；答案卡片长代码块未做等宽渲染（当前为纯文本气泡）。

## 6. 响应式与可达性（3）
- 全局 `:focus-visible` 主色焦点环；标签/按钮补 `aria-label`、`role=tab`/`aria-selected` ✅。
- `aria-live="polite"` 状态胶囊支持读屏播报。
- 窄屏（≤720px）侧栏转为浮层可收起，模型选择隐藏以省空间。
- 待优化：窄屏下顶栏标题隐藏后品牌识别变弱；模型选择隐藏后无法在移动端切模型，可改为折叠进「更多」或设置页。

---

## 建议修复项（按优先级）
1. **[中] 浅色思考步骤对比**：`.step.thought` 文字改回 `--text`，仅保留左侧 `--amber` 色条。
2. **[中] 长内容换行**：`.step`、`.confirm-card pre` 增加横向溢出提示或软换行，避免破坏布局。
3. **[低] 窄屏模型切换**：移动端隐藏 `model-select` 后，应在设置或分段控件内提供替代入口。
4. **[低] 答案代码块**：检测 Markdown/代码围栏，渲染为等宽 pre 提升可读性。

---

## 验收
- [x] UI-SPEC.md 设计契约已落地到 HTML/CSS/JS
- [x] 三主题（跟随系统/浅/深）仍可用
- [x] 场景切换、文件浏览、发送、确认、设置 全链路保留
- [x] 新增：侧栏折叠、空状态、模型选择器、答案复制、新对话
- [x] 6 维审计完成，综合 3.5/4
